---
import { Icon } from 'astro-icon/components';
import type { CollectionEntry } from 'astro:content';
import { contentTypes, type EntryCardStyle } from '../../config/content';
import type { Locale } from '../../config/i18n';
import { entryDate, formatDate, localizedEntryPath } from '../../lib/content/entries';

interface Props {
  entry: CollectionEntry<'posts'> | CollectionEntry<'projects'>;
  collection: keyof typeof contentTypes;
  locale: Locale;
  style?: EntryCardStyle;
}

const { entry, collection, locale, style } = Astro.props;
const config = contentTypes[collection];
const cardStyle = style ?? config.cardStyle;
const href = localizedEntryPath(collection, entry as any);
const date = entryDate(entry as any);
const tags = entry.data.tags?.slice(0, cardStyle === 'showcase' ? 3 : 4) ?? [];
const hasDate = config.showMeta && date.getTime() > 0;
const isFeatured = 'featured' in entry.data && entry.data.featured;
const links = 'links' in entry.data ? (entry.data.links?.slice(0, 2) ?? []) : [];
---

{
  cardStyle === 'showcase' ? (
    <article class="group h-full">
      <a href={href} class="block h-full">
        <div class="border-border bg-card hover:border-primary/25 focus-visible:ring-primary/25 relative flex h-full min-h-64 flex-col overflow-hidden rounded-[var(--radius-card)] border p-6 shadow-[var(--surface-shadow)] transition-all duration-300 ease-out hover:-translate-y-1 hover:shadow-[var(--surface-shadow-strong)] focus-visible:ring-2 focus-visible:outline-none">
          {entry.data.cover ? (
            <>
              <img
                src={entry.data.cover}
                alt=""
                class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/60 to-black/25" />
            </>
          ) : (
            <div class="bg-primary/5 absolute inset-0 opacity-0 transition-opacity duration-300 group-hover:opacity-100" />
          )}

          <div class="relative z-10 flex min-h-52 flex-1 flex-col">
            <div class="flex items-start justify-between gap-3">
              <span
                class:list={[
                  'grid h-8 w-8 place-items-center rounded-full border text-sm',
                  entry.data.cover
                    ? 'border-white/15 bg-white/15 text-white backdrop-blur-sm'
                    : 'border-border bg-muted text-primary'
                ]}
              >
                <Icon name={config.icon} class="h-4 w-4" />
              </span>
              {isFeatured && (
                <span
                  class:list={[
                    'grid h-8 w-8 place-items-center rounded-full',
                    entry.data.cover
                      ? 'bg-white/15 text-white backdrop-blur-sm'
                      : 'bg-primary/10 text-primary'
                  ]}
                >
                  <Icon name="lucide:star" class="h-4 w-4" />
                </span>
              )}
            </div>

            <div class="mt-auto">
              <h2
                class:list={[
                  'truncate text-xl leading-tight font-semibold transition-colors',
                  entry.data.cover ? 'text-white' : 'text-foreground group-hover:text-primary'
                ]}
              >
                {entry.data.title}
              </h2>
              {entry.data.description && (
                <p
                  class:list={[
                    'mt-2 line-clamp-2 text-sm leading-6',
                    entry.data.cover ? 'text-white/80' : 'text-muted-foreground'
                  ]}
                >
                  {entry.data.description}
                </p>
              )}

              <div class="mt-4 flex flex-wrap items-center gap-2">
                {tags.map((tag) => (
                  <span
                    class:list={[
                      'rounded border px-2.5 py-0.5 text-xs font-medium',
                      entry.data.cover
                        ? 'border-white/10 bg-white/15 text-white backdrop-blur-sm'
                        : 'border-border bg-muted text-muted-foreground'
                    ]}
                  >
                    {tag}
                  </span>
                ))}
                {links.length > 0 && (
                  <span
                    class:list={[
                      'ml-auto inline-flex items-center gap-1 text-xs font-medium',
                      entry.data.cover ? 'text-white/80' : 'text-muted-foreground'
                    ]}
                  >
                    {links.length}
                    <Icon name="lucide:link" class="h-3.5 w-3.5" />
                  </span>
                )}
              </div>
            </div>
          </div>
        </div>
      </a>
    </article>
  ) : cardStyle === 'compact' ? (
    <article class="border-border border-b py-4">
      <a href={href} class="group flex items-center justify-between gap-4">
        <div class="min-w-0">
          <h2 class="group-hover:text-primary truncate text-base font-medium">
            {entry.data.title}
          </h2>
          {hasDate && (
            <time datetime={date.toISOString()} class="text-muted-foreground mt-1 block text-xs">
              {formatDate(date, locale)}
            </time>
          )}
        </div>
        <Icon
          name="lucide:arrow-right"
          class="text-muted-foreground group-hover:text-primary h-4 w-4 shrink-0 transition-transform group-hover:translate-x-1"
        />
      </a>
    </article>
  ) : (
    <article class="group">
      <a
        href={href}
        class="border-border bg-card hover:border-primary/25 hover:bg-primary/5 focus-visible:ring-primary/25 block rounded-[var(--radius-card)] border transition-all duration-300 ease-out hover:-translate-y-1 hover:shadow-[var(--surface-shadow)] focus-visible:ring-2 focus-visible:outline-none"
      >
        <div class="flex flex-col overflow-hidden rounded-[inherit] md:flex-row">
          <div class="flex flex-1 flex-col justify-center gap-4 p-5 sm:p-6">
            <h2 class="group-hover:text-primary text-xl leading-tight font-semibold transition-colors">
              {entry.data.title}
            </h2>
            {entry.data.description && (
              <p class="text-muted-foreground line-clamp-3 text-sm leading-6 md:line-clamp-2">
                {entry.data.description}
              </p>
            )}

            <div class="text-muted-foreground flex flex-wrap items-center gap-x-4 gap-y-2 text-sm">
              {hasDate && (
                <span class="inline-flex items-center gap-1.5">
                  <Icon name="lucide:calendar" class="h-4 w-4" />
                  <time datetime={date.toISOString()}>{formatDate(date, locale)}</time>
                </span>
              )}
              {tags.length > 0 && (
                <span class="flex flex-wrap items-center gap-1.5">
                  {tags.map((tag) => (
                    <span class="border-border bg-muted/60 rounded-md border px-2 py-0.5 text-xs">
                      {tag}
                    </span>
                  ))}
                </span>
              )}
            </div>
          </div>

          {entry.data.cover && (
            <div class="order-first p-3 pb-0 md:order-last md:w-56 md:shrink-0 md:self-center md:p-3 md:pl-0">
              <div class="bg-muted aspect-video overflow-hidden rounded-[var(--radius-panel)]">
                <img
                  src={entry.data.cover}
                  alt=""
                  class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
                  loading="lazy"
                />
              </div>
            </div>
          )}
        </div>
      </a>
    </article>
  )
}
